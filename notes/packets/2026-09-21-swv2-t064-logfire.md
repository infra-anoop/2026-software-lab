# Work packet — SW V2 T064 Logfire + job elapsed/usage (D3)

## Meta

| Field | Value |
|-------|-------|
| Packet id | `2026-09-21-swv2-t064-logfire` |
| Status | done |
| Feature / spec | `specs/smart-writer-v2/` |
| Branch | `packet/2026-09-21-swv2-t064-logfire` |
| Agent mode | **background** |
| Spawn | `docs/agent-os/SPAWN_WORKER.md` + `_WORKER_PROMPT.md` |

## Goal

Wire Logfire (noop if unset) and put `elapsed_ms` + `usage` on terminal job snapshots so dogfood can debug spend/time (**D3** / **T064**).

## Context to read first

- `AGENTS.md`
- `specs/smart-writer-v2/spec.md` — **D3**
- `specs/smart-writer-v2/contracts/http-api.md` — job snapshot `elapsed_ms` / `usage`
- `specs/smart-writer-v2/data-model.md` — Job metrics
- `specs/smart-writer-v2/tasks.md` — **T064**
- Existing job execution path: `apps/smart-writer-v2/app/entrypoints/http.py`, orchestrator graphs
- Pattern notes: V1 may have Logfire wiring — reuse ideas only, clean-room in V2

## Owned paths (may edit)

- `apps/smart-writer-v2/app/obs.py` (**new**)
- `apps/smart-writer-v2/app/entrypoints/http.py` (lifespan + job snapshot fields)
- `apps/smart-writer-v2/app/orchestrator/` (best-effort token/elapsed capture if needed)
- `apps/smart-writer-v2/app/config.py` if Settings needs Logfire helpers
- `apps/smart-writer-v2/tests/` (contract: terminal job JSON has `elapsed_ms` + `usage` keys)
- `specs/smart-writer-v2/tasks.md` (T064 checkbox only when proven)
- `notes/packets/2026-09-21-swv2-t064-logfire.md`

## Forbidden paths (do not edit)

- Re-open D5 / UI cattle / BFF
- Uploads T073–T076 (next packet)
- Committing secret values; logging secrets or raw `InternalRunState`
- Expanding Codespace Actions permissions

## Parallelism

| Field | Value |
|-------|-------|
| `[P]` tasks in this packet | no |
| Disjoint from other in-flight packets? | n/a — serial before uploads |

## Definition of Done

- [x] `app/obs.py`: init Logfire when `LOGFIRE_TOKEN` set; **noop if unset**; product still runs
- [x] Lifespan (or equivalent) hooks observability
- [x] Job spans (or minimal equivalent) around write jobs — no secrets / no raw InternalRunState
- [x] Terminal job snapshot JSON **always includes** keys `elapsed_ms` and `usage` (`input_tokens`, `output_tokens`, optional `estimated_cost_usd`) per contract — best-effort numbers preferred
- [x] Contract test(s) assert those keys on a terminal job
- [x] `uv run ruff check app/ tests/` + `uv run pytest` green under `nix develop`
- [x] T064 checked in `tasks.md`
- [x] Commit on packet branch; push; open PR or note merge-ready SHA
- [x] Handoff filled

## Out of scope

- T073–T076 uploads
- T077 final polish (after uploads)
- Seeding `LOGFIRE_TOKEN` in Infisical (optional; noop path must work without it)
- Prod ship/deploy unless trivial

## Governor locks required

| Lock | Value |
|------|--------|
| D3 | **locked** — wire Logfire; elapsed + tokens/cost on jobs; noop if unset |
| FINISH_BAR | Implement unblocked: yes |
| PLAN_DELTA | D3 already in §G.1 batch |

## Fidelity (constitution §I)

| Lock | fidelity | How this packet honors it |
|------|----------|---------------------------|
| D3 | letter | Real Logfire init when token present; snapshot keys required; noop without token |

## Stop / escalate if

- Provider usage unavailable → still emit keys with best-effort/`null` per contract; do not invent fake token burns
- Missing product decision on cost formula → use best-effort or omit `estimated_cost_usd` only (other keys required)

## Handoff notes (agent fills at end)

- **Branch:** `packet/2026-09-21-swv2-t064-logfire`
- **SHA:** (filled after commit)
- **Tests:** `uv run ruff check app/ tests/` clean; `uv run pytest` → 61 passed under `nix develop`
- **T064:** checked in `tasks.md`
- **Behavior:** `app/obs.py` configures Logfire + `instrument_pydantic_ai` when `LOGFIRE_TOKEN` set; noop otherwise. Lifespan calls configure. Write jobs wrap `swv2.write_job` spans (mode + conversation_id only). Terminal snapshots always include `elapsed_ms` (from JobRunner timestamps) and `usage` with `input_tokens` / `output_tokens` / `estimated_cost_usd` — token fields null when provider totals unavailable (agents return outputs only; no fake burns).
- **Remaining:** T073–T076 uploads; T077 polish; optional Infisical seed for `LOGFIRE_TOKEN` (noop path already works).
- **PR:** (filled after push)
