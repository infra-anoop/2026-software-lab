# Packet: 2026-09-20-swv2-phase10-t063

## Meta

| Field | Value |
|-------|-------|
| Packet id | `2026-09-20-swv2-phase10-t063` |
| Status | done |
| Feature / spec | `specs/smart-writer-v2/` |
| Branch | `main` (or `packet/2026-09-20-swv2-phase10-t063` if preferred) |
| Agent mode | **background** |
| Spawn | `docs/agent-os/SPAWN_WORKER.md` + `_WORKER_PROMPT.md` |

## Goal

Enforce locked spend caps so one chat cannot run unbounded write jobs or clarifying turns; honor the inner-turn max of 8 in Settings for the quality loop.

## Context to read first

- `AGENTS.md`
- `docs/agent-os/SPAWN_WORKER.md`
- `specs/smart-writer-v2/spec.md` (Open Decisions D2 locked; FR-022)
- `specs/smart-writer-v2/plan.md`
- `specs/smart-writer-v2/FINISH_BAR.md` (Implement unblocked: yes)
- `specs/smart-writer-v2/PLAN_DELTA.md`
- `specs/smart-writer-v2/tasks.md` Phase 10 — **T063**
- `specs/smart-writer-v2/contracts/http-api.md` (Errors / caps if documented)
- Existing rate-limit pattern in `apps/smart-writer-v2/app/entrypoints/http.py` + `tests/contract/test_rate_limit.py`

## Owned paths (may edit)

- `apps/smart-writer-v2/app/config.py`
- `apps/smart-writer-v2/app/entrypoints/http.py`
- `apps/smart-writer-v2/app/store.py` (counters if needed)
- `apps/smart-writer-v2/tests/contract/` (new or extend cap tests)
- `apps/smart-writer-v2/tests/unit/` if helpers need tests
- `deploy/secrets/schema.yaml` only if new **names** for cap env knobs are required
- `specs/smart-writer-v2/tasks.md` (check off T063 only)

## Forbidden paths (do not edit)

- Secret **values** / `.env` with real secrets
- Quality-loop agents/graphs (T078+) except reading Settings for max 8
- `apps/smart-writer-v2/web/` (Settings UI = later)
- Other apps

## Parallelism

| Field | Value |
|-------|-------|
| `[P]` tasks in this packet | no |
| Disjoint from other in-flight packets? | yes (vs vault HITL / deploy ops) |

## Definition of Done

- [x] Settings (or env-backed helpers) expose: max **3** write jobs / conversation; max **10** clarify turns / conversation; max **8** inner assessor turns / write job (defaults = locked numbers; override via env OK if named in schema when added)
- [x] Fail closed when outer caps hit (appropriate HTTP error — prefer contract-aligned 429 or documented 409; do not invent silently)
- [x] Contract tests: red then green for outer caps (write-job cap + clarify-turn cap). Inner max 8 may be Settings-only until D8 loop lands — document that in test docstring if not enforced in graph yet
- [x] `cd apps/smart-writer-v2 && uv run ruff check app/ tests/` and `uv run pytest` green (use app `.venv` if needed)
- [x] T063 checked off in `tasks.md`
- [x] Commit with a clear message (no force-push; no `--no-verify`)

## Out of scope

- Infisical vault seed (T070 — human)
- Railway ship/deploy (T071)
- Dual-axis scored loop impl (T078–T086) beyond Settings knob for max 8
- Logfire (T064), Settings UI (T065), uploads, v0 GUI

## Governor locks required

| Lock | Value |
|------|--------|
| FINISH_BAR | Implement unblocked: yes |
| D2 | 3 write jobs / 10 clarifies / 8 inner turns |
| D6 vault | not required for this local packet |

## Stop / escalate if

- Unclear which HTTP status for cap hit and contract is silent — pick one, document in http-api.md, note in handoff
- DoD needs web UI or quality-loop graph rewrite

## Handoff notes (agent fills at end)

- What changed:
  - Settings getters + env knobs (defaults 3 / 10 / 8): `SMART_WRITER_V2_MAX_WRITE_JOBS_PER_CONVERSATION`, `…_MAX_CLARIFY_TURNS_PER_CONVERSATION`, `…_MAX_INNER_ASSESSOR_TURNS` in `config.py` / `.env.example` (cattle companion for `ALL_ENV_NAMES`; not vault secrets).
  - Per-conversation counters on `InternalRunState`; `POST .../messages` fail-closed **429** when clarify or write-job cap hit (aligned with http-api.md rate/spend `429`; no contract amend needed).
  - Contract tests `test_conversation_caps.py`; unit Settings defaults + inner-max docstring (Settings-only until D8).
  - T063 checked in `tasks.md`.
- Tests run:
  - `.venv/bin/ruff check app/ tests/` — pass
  - `.venv/bin/pytest` — **51 passed**
- Open questions for human:
  - None. Cap hit uses **429** (same family as rate limit). Inner max 8 not enforced in graph yet (T083+).
