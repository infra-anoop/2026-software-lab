# Packet: 2026-09-20-swv2-phase10-t081-t085

## Meta

| Field | Value |
|-------|-------|
| Packet id | `2026-09-20-swv2-phase10-t081-t085` |
| Status | ready → **done** (worker) |
| Feature / spec | `specs/smart-writer-v2/` |
| Branch | `main` OK (or `packet/2026-09-20-swv2-phase10-t081-t085`) |
| Agent mode | **background** |
| Spawn | `docs/agent-os/SPAWN_WORKER.md` + `_WORKER_PROMPT.md` |

## Goal

Ship the dual-axis scored writer↔assessor inner loop on generate and revise so job snapshots expose real `rubric_id` + `loop` and the T079 contract tests go green (with T47/T48 consistency locks).

## Context to read first

- `AGENTS.md`, `docs/agent-os/SPAWN_WORKER.md`
- `specs/smart-writer-v2/{spec,plan,FINISH_BAR,PLAN_DELTA,data-model,contracts/http-api,acceptance,tasks}.md`
- D8 / FR-022; D2 inner max 8 via `get_max_inner_assessor_turns`
- `specs/smart-writer-v2/TEST_REVIEW.md` — **T080 adjudication**: T47/T48 locked to shape+consistency + dual-axis dimension coverage
- `apps/smart-writer-v2/app/orchestrator/generate_graph.py`, `revise_graph.py`
- Existing agents under `apps/smart-writer-v2/app/agents/` (schema-first Pydantic `result_type`)
- Red tests: `tests/contract/test_scored_loop_job.py`, `tests/unit/test_scored_loop_generate.py`

## Owned paths (may edit)

- `apps/smart-writer-v2/app/agents/` (rubric builder, assessor)
- `apps/smart-writer-v2/app/orchestrator/generate_graph.py`
- `apps/smart-writer-v2/app/orchestrator/revise_graph.py`
- `apps/smart-writer-v2/app/` models / assembly / http job snapshot as needed for `rubric_id` + `loop`
- `apps/smart-writer-v2/app/config.py` only if clamping inner max to 8 (T51 Later)
- `specs/smart-writer-v2/tasks.md` (check T081–T085, T080, T086 if catalog auto rows)
- `specs/smart-writer-v2/acceptance.md` (T086 structural `how: auto` only)
- `notes/packets/2026-09-20-swv2-phase10-t081-t085.md` (handoff)

## Forbidden paths (do not edit)

- Re-open T47/T48 product Debates
- Stub `_execute_job` / graph to return assertion JSON
- Vault secrets, deploy cattle, other apps
- T064 Logfire, T065 Settings UI, T069–T076, T087 (out of this packet)

## Parallelism

| Field | Value |
|-------|-------|
| `[P]` tasks in this packet | no (serial T081→T085) |
| Disjoint from other in-flight? | n/a |

## Definition of Done

- [x] T081: Rubric from Axis A (intent) + Axis B (properties); not thinner than V1 scored capability
- [x] T082: Assessor agent with schema-first scores (`result_type`)
- [x] T083: generate graph: after research, rubric → write ↔ assess (≤ Settings max, default 8)
- [x] T084: revise graph runs the same scored inner loop (research may narrow/skip)
- [x] T085: persist/expose `rubric_id` + `loop` on job snapshot; green T079 tests including T47/T48 consistency
- [x] T086: mark structural catalog rows `how: auto` where honest; leave score-quality hybrid/human
- [x] `cd apps/smart-writer-v2 && uv run pytest` green; `uv run ruff check app/ tests/`
- [x] Do **not** invent canned loop JSON that violates stop↔iterations or dual-axis coverage
- [ ] Commit; fill handoff; check tasks.md boxes

## Out of scope

- T064 observability, T065 citation Settings UI, T069 Railway UI, uploads, D9 regenerate control polish
- Live LLM quality as auto SC

## Governor locks required

| Lock | Value |
|------|--------|
| FINISH_BAR | Implement unblocked: yes |
| D8 | dual-axis scored loop required; ≤8; not thinner than V1 |
| D2 inner | Settings max 8 |
| T47 | shape + stop↔iterations consistency |
| T48 | per-turn `len(dimension_scores) >= axis_a + axis_b` (+ distinct ids) |
| T49 | HTTP is gate; `run_generate` may return loop keys |

## Stop / escalate if

- Tempted to green by fabricating loop without real assess path
- Product ambiguity beyond locked T47/T48 — escalate; do not invent new OD content

## Handoff notes (agent fills at end)

- What changed:
  - T081: `app/agents/rubric_builder.py` builds dual-axis rubric from intent slots + property ranking (≥1 dim/axis).
  - T082: `app/agents/assessor.py` schema-first `AssessorOutput`; LLM when live key, else heuristic assess path covering all rubric dims.
  - T083/T084: `orchestrator/scored_loop.py` shared write↔assess ≤ Settings max (clamped to 8); wired in generate (after research) and revise (research skipped).
  - T085: `GenerateResult` + job snapshot expose `rubric_id` + `loop`; `_execute_job` / `GET /jobs` persist them.
  - T086: catalog `loop.*` / `rubric.dual_axis` confirmed `how: auto`; score-quality remains hybrid/human.
  - T51 Later: `get_max_inner_assessor_turns` clamps to ≤8.
- Tests run: `apps/smart-writer-v2` — 55 passed; ruff clean after fix.
- Open questions for human: none (no escalate).
