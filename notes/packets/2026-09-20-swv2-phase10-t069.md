# Packet: 2026-09-20-swv2-phase10-t069

## Meta

| Field | Value |
|-------|-------|
| Packet id | `2026-09-20-swv2-phase10-t069` |
| Status | **blocked / pivoted** — human runs real Vercel v0; brief `notes/packets/2026-09-20-swv2-v0-brief.md`. Prior worker stopped. |
| Feature / spec | `specs/smart-writer-v2/` |
| Branch | `main` OK |
| Agent mode | **background** |
| Spawn | `docs/agent-os/SPAWN_WORKER.md` + `_WORKER_PROMPT.md` |

## Goal

Ship the chat + Settings UI as a **Railway-hosted** product surface (BFF holds the audit secret), with a **v0-class** visual design pass on the existing Next app. Local/Codespaces remain dev-only.

## Context to read first

- `AGENTS.md`, `docs/agent-os/SPAWN_WORKER.md`
- D5 lock / plan Topology (P1 amended): v0 **design**, Railway **deploy**; worker stays FastAPI service
- `deploy/railway/production/smart-writer-v2.yml`, `deploy/railway/README.md`
- `apps/registry.yaml` (`smart-writer-v2`)
- `apps/smart-writer-v2/web/` (post-T065 Settings panel)
- `notes/packets/2026-09-20-ops-runtime-tags.md` if present (ops tag path for provision/sync)
- `deploy/secrets/schema.yaml` — UI may need same audit secret name + worker URL (URL is config, not secret)

## Architecture lock for this packet (implement; do not re-debate)

| Choice | Lock |
|--------|------|
| Service split | **Two Railway services** in project `2026-software-lab`: existing `smart-writer-v2` (API) + new `smart-writer-v2-ui` (Next) |
| Image | UI: Dockerfile/`standalone` Next build in git; pin via lab deploy pattern or document first-ship path in README if registry ship is worker-only today |
| Secrets | BFF env on UI service: `SMART_WRITER_V2_AUDIT_SECRET` (sync from Infisical) + `SMART_WRITER_V2_WORKER_URL` (public worker URL — may be plain Railway variable, not vault) |
| Design | **v0-class** pass in-repo on chat + Settings (expressive typography, atmosphere, one composition — honor lab frontend rules). Literal Vercel v0 export is optional human follow-up, not a blocker if in-repo design meets D5 “designed with v0-class tools” spirit |
| Browser | Never `NEXT_PUBLIC_*` for audit secret |

## Fidelity (constitution §I)

| Lock | fidelity | How this packet honors it |
|------|----------|---------------------------|
| D5 design with Vercel v0 | letter | **Pivot 2026-09-20:** human runs real v0 using `notes/packets/2026-09-20-swv2-v0-brief.md`. Prior “v0-class in-repo” waive withdrawn for finish of T069(1). |
| D5 Railway UI deploy | letter | After v0 export lands; cattle drafts (`Dockerfile`, `smart-writer-v2-ui.yml`) may already be on disk uncommitted |

## Owned paths (may edit)

- `apps/smart-writer-v2/web/` (design pass + Dockerfile / next config for prod)
- `deploy/railway/production/smart-writer-v2-ui.yml` (**new**)
- `deploy/railway/README.md`
- `apps/smart-writer-v2/web/README.md` (Railway host, not Vercel deploy)
- `apps/smart-writer-v2/README.md` (pointer to UI URL when known)
- `apps/registry.yaml` / `registry.json` **only if** required for UI ship/deploy cattle — prefer minimal; document if UI is provisioned via YAML + manual first image
- `deploy/secrets/schema.yaml` **only if** schema must name UI-side vars (names only)
- `specs/smart-writer-v2/tasks.md` (T069 checkbox)
- `notes/packets/2026-09-20-swv2-phase10-t069.md` (handoff)

## Forbidden paths (do not edit)

- Folding UI into the FastAPI worker container as a permanent “hack” without documenting (two-service lock above)
- Vercel project provision as production host
- Committing secret values
- T064 Logfire, T073 uploads, T087 (unless regenerate control is a one-line Settings/header affordance already implied by D9 — prefer leave T087)
- Re-opening D5

## Parallelism

| Field | Value |
|-------|-------|
| `[P]` | no vs uploads |
| Disjoint from ops-runtime-tags? | mostly yes; avoid editing same README sections simultaneously — prefer append |

## Definition of Done

- [ ] v0-class visual pass on chat + Settings (readable, intentional; not a purple-gradient AI sludge default)
- [ ] `deploy/railway/production/smart-writer-v2-ui.yml` declared; README documents service + env names + worker URL
- [ ] Production-ready Next build path (Dockerfile or nix — pick one that fits lab; Docker OK for Node UI if nix not ready)
- [ ] BFF still server-only secret; README says Railway UI host (rewrite Vercel-as-host language)
- [ ] T069 checked; commit; handoff lists **human ops** to provision/sync/deploy UI if agent cannot push tags/dispatch (print exact helper/UI steps)
- [ ] If a public UI URL is not live yet: leave checkbox note + handoff “awaiting human provision/deploy” but **cattle + design must be in git**

## Out of scope

- Literal paid Vercel v0 session (human may refine later)
- Uploads (T073+)
- Logfire (T064)
- Changing worker loop behavior

## Governor locks required

| Lock | Value |
|------|--------|
| D5 | v0-class design; Railway UI deploy; BFF custody |
| D4 | Settings panel already shipped — refine layout OK |
| Two services | `smart-writer-v2` + `smart-writer-v2-ui` |

## Stop / escalate if

- Registry/ship matrix cannot express a Node UI without inventing a large new platform epic — escalate with A/B (Docker-only UI service vs expand registry)
- Tempted to make Codespaces the production URL

## Handoff notes (agent fills at end)

- What changed:
- Human ops next (provision/sync/deploy commands):
- Public UI URL (or pending):
- Open questions for human:
