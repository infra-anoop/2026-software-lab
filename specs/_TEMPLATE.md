# DEPRECATED — use Spec Kit template

Canonical template: `.specify/templates/overrides/spec.md` (lab) wrapping Spec Kit practice.
Create specs with skill **`speckit-specify`**.

See `docs/agent-os/README.md`.
