# apps/research-auditor/nixpacks.toml

[phases.setup]
# Adding python3 here ensures it is available in the final container
nixPkgs = ["python3"]

[start]
# We no longer need 'nix develop' here because the environment is already built
cmd = "python3 audit_env.py"
