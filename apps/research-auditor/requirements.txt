# This file signals to Railway that this is a Python app
# --- Security & Auditing ---
pip-audit==2.10.0      # Scans dependencies for known vulnerabilities
safety==3.2.0          # Additional security vulnerability checking

# --- Web & API (If your auditor checks remote environments) ---
requests==2.32.3       # Standard for making HTTP requests
httpx==0.27.0          # Modern, async-ready HTTP client

# --- Data & Environment Analysis ---
#pandas==2.2.2          # For structured data analysis of audit results
python-dotenv==1.0.1   # Loads variables from .env if running locally
pyyaml==6.0.1          # For parsing config files (common in research)

# --- Monitoring & Utilities ---
psutil==5.9.8          # To audit system-level environment (CPU, RAM, Processes)