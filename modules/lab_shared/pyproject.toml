[project]
name = "lab-shared"
version = "0.1.0"
description = "Shared infrastructure for 2026-software-lab apps (HTTP job runner + RunRepo/db)"
requires-python = ">=3.12,<3.13"
dependencies = [
  "supabase>=2.28,<3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
